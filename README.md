# PflegeAssist Web

Öffentliche Web-Oberfläche für PflegeAssist (nur UI, kein Backend-Code).
Quelle: privates Repo Marcel-Fe/pflegeassist (Ordner `pflegeassist/frontend`).

Beim ersten Öffnen die Server-Adresse (API) eintragen (z. B. die Render-URL des Backends).
